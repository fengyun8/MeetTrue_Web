.content
  width 100vw
  max-width max-screen
  margin 0 auto
  .surface
    top 0
    height 100vh
    width 100%
    box-sizing border-box
    &:after
      clear both
    &:after, &:before
      display table
      content " "
